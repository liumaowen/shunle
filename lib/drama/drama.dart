import 'package:flutter/material.dart';

class Drams extends StatelessWidget { 
  const Drams({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Drams'),
      ),
    );
  }
}